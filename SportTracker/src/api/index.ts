// API configuration and endpoints
export const API_BASE_URL = 'https://dummyjson.com';

export const endpoints = {
  auth: {
    login: `${API_BASE_URL}/auth/login`,
    register: `${API_BASE_URL}/users/add`,
  },
  // Add more endpoints as needed
};
